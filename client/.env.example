# Copier en .env (dev local hors Docker) ou .env.local
# Docker: docker-compose.yml definit deja VITE_API_URL, ce fichier sert
# surtout si tu lances "npm run dev" directement sur ta machine.
VITE_API_URL=http://localhost:5000/api
